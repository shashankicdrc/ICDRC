version: '3'
services:
  frontend:
    image: abhishekdev45/icdrc-client:latest
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    image: abhishekdev45/icdrc-server:latest
    ports:
      - "5000:5000"
    environment:
      - MONGO_URL=${MONGO_URL}
      - JWT_SECRET=${JWT_SECRET}
